import { render, screen } from '@testing-library/react'
import Map from './map'

describe('Map', () => {
  it('example unit test', () => {
    // const gameMap = [['.', '.', '.'], ['.', '*', '.'], ['.', '.', '*']];
    // render(<Map gameMap={gameMap} />)

  });
});